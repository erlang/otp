
  [;1m-spec is_bitstring(Term) -> boolean() when Term :: term().[0m

  Returns [;;4mtrue[0m if [;;4mTerm[0m is a bitstring (including a binary),
  otherwise [;;4mfalse[0m.

  Allowed in guard tests.
